{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "{{ ID }}",
	"title": "Global Address",
	"description": "A flexible structure for international addresses",
	"type": "object",
	"required": [
		"addressLine1",
		"locality",
		"countryCode"
	],
	"properties": {
		"addressLine1": {
			"type": "string",
			"description": "Street address, P.O. box, company name, c/o"
		},
		"addressLine2": {
			"type": "string",
			"description": "Apartment, suite, unit, building, floor, etc."
		},
		"locality": {
			"type": "string",
			"description": "City, town, village, or suburb"
		},
		"administrativeArea": {
			"type": "string",
			"description": "State, province, region, prefecture, or territory"
		},
		"postcode": {
			"type": "string",
			"description": "Postal code or ZIP code"
		},
		"countryCode": {
			"type": "string",
			"description": "ISO 3166-1 alpha-2 country code",
			"pattern": "^[A-Z]{2}$"
		}
	}
}